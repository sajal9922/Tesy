## How to register a user
    -Fill the form as admin
    -React component sends json to backend
        {   username,
            realname,
            email   }


    -Backend uses a library to connect to db
     and sends query to db with pg library
     


    -Database responds
